return --[[@type (LazyPluginSpec[])]]{
  {
    'vhyrro/luarocks.nvim',
    priority = 1000,
    opts = { -- Put the names of rocks from luarocks.org here:
      -- rocks = {
      --   'json.lua'
      -- }
    }
  }
}
